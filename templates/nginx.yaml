kind: DockerDeployment
metadata:
    name: {{ name }}
    labels:
        app: helloworld
        example: "true"
spec:
    replicas: {{ nginxReplicas }}
    instances:
        - name: helloworld
          type: container
          image: nginx:alpine
          environment: {}
