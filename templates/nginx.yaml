kind: ExecutorDeployment
metadata:
    colonyname: {{ colonyName }}
    name: web
spec:
    executorType: "thisisatest"
    image: nginx:latest
    replicas: {{ nginx.replicas }}
    ports:
        - name: http
          port: {{ nginx.port }}
