kind: ExecutorDeployment
metadata:
    colonyname: {{ colonyName }}
    name: web
handler:
    executortype: "docker-reconciler"
spec:
    image: nginx:latest
    replicas: {{ nginx.replicas }}
    ports:
        - name: http
          port: {{ nginx.port }}
